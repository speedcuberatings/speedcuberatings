export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: '2rem', maxWidth: 720 }}>
      <h1>Speedcube Ratings</h1>
      <p>
        Under construction. Phase 1 (WCA ingest) is live — the site itself will
        land once derived tables and rating computation are in place.
      </p>
    </main>
  );
}
